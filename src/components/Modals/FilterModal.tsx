import { useRef } from "react";
import style from '../../styles/dashoboard.module.scss';

const FilterModal = ({ show, setShow }: any) => {
  return <div className={`${style.filterCon}`}>FilterModal</div>;
};

export default FilterModal;
